matrix:
  include:

  # CentOS 6, Qt 5.6, Python 2.7
  - os: linux
    dist: trusty
    sudo: required
    language: python
    python: 2.7
    services:
    - docker
    env:
    - DOCKER_OS=centos6
    - QT_VER=5.6

  # CentOS 6, Qt 5.6, Python 3.5
  - os: linux
    dist: trusty
    sudo: required
    language: python
    python: 3.5
    services:
    - docker
    env:
    - DOCKER_OS=centos6
    - QT_VER=5.6

  # CentOS 7, Qt 5.6, Python 2.7
  - os: linux
    dist: trusty
    sudo: required
    language: python
    python: 2.7
    services:
    - docker
    env:
    - DOCKER_OS=centos7
    - QT_VER=5.6

  # CentOS 7, Qt 5.6, Python 3.5
  - os: linux
    dist: trusty
    sudo: required
    language: python
    python: 3.5
    services:
    - docker
    env:
    - DOCKER_OS=centos7
    - QT_VER=5.6

  # Ubuntu 14.04, Qt 5.6, Python 2.7
  - os: linux
    dist: trusty
    sudo: required
    language: python
    python: 2.7
    services:
    - docker
    env:
    - DOCKER_OS=ubuntu14.04
    - QT_VER=5.6

  # Ubuntu 14.04, Qt 5.6, Python 3.5
  - os: linux
    dist: trusty
    sudo: required
    language: python
    python: 3.5
    services:
    - docker
    env:
    - DOCKER_OS=ubuntu14.04
    - QT_VER=5.6

  # Ubuntu 16.04, Qt 5.6, Python 2.7
  - os: linux
    dist: trusty
    sudo: required
    language: python
    python: 2.7
    services:
    - docker
    env:
    - DOCKER_OS=ubuntu16.04
    - QT_VER=5.6

  # Ubuntu 16.04, Qt 5.6, Python 3.5
  - os: linux
    dist: trusty
    sudo: required
    language: python
    python: 3.5
    services:
    - docker
    env:
    - DOCKER_OS=ubuntu16.04
    - QT_VER=5.6

  # OS X 10.10, Xcode 6.4, Qt 5.6, Python 2.7 (pip missing)
  - os: osx
    osx_image: xcode6.4
    language: generic
    python: 2.7
    cache:
      pip: true
      # directories:
      #   - $HOME/Library/Caches/Homebrew
    env:
    - RVM_CERT_FIX=1
    - PYTHON_VERSION=2.7
    - QT_VER=5.6

  # OS X 10.10, Xcode 6.4, Qt 5.6, Python 3.5
  - os: osx
    osx_image: xcode6.4
    language: generic
    python: 3.5
    cache:
      pip: true
      # directories:
      #   - $HOME/Library/Caches/Homebrew
    env:
    - RVM_CERT_FIX=1
    - PYTHON_VERSION=3.5
    - QT_VER=5.6

  # Disabled builds below

  # # OS X 10.11, Xcode 7.3, Qt 5.6, Python 2.7
  # - os: osx
  #   osx_image: xcode7.3
  #   language: generic
  #   python: 2.7
  #   cache:
  #     pip: true
  #     # directories:
  #     #   - $HOME/Library/Caches/Homebrew
  #   env:
  #   - PYTHON_VERSION=2.7
  #   - QT_VER=5.6

  # # OS X 10.11, Xcode 7.3, Qt 5.6, Python 3.5
  # - os: osx
  #   osx_image: xcode7.3
  #   language: generic
  #   python: 3.5
  #   cache:
  #     pip: true
  #     # directories:
  #     #   - $HOME/Library/Caches/Homebrew
  #   env:
  #   - PYTHON_VERSION=3.5
  #   - QT_VER=5.6


before_install:
  - pwd
  - if [ "$TRAVIS_OS_NAME" == "linux" ]; then sudo apt-get install -y tree ; fi
  - if [ "$TRAVIS_OS_NAME" == "osx" ]; then brew install tree ; fi
  - tree
  # Workaround on OS X for Travis issue https://github.com/travis-ci/travis-ci/issues/6700
  # Note - these workarounds are disabled as they seem to no longer be necessary
  # Generic workaround:
  # - if [ "$TRAVIS_OS_NAME" == "osx" ]; then rvm reinstall 1.9.3 --disable-binary ; fi
  # Workaround for xcode6.4 only:
  # - if [ "$TRAVIS_OS_NAME" == "osx" ] && [ "$RVM_CERT_FIX" == "1" ]; then sudo security delete-certificate -Z 2F173F7DE99667AFA57AF80AA2D1B12FAC830338 /System/Library/Keychains/SystemRootCertificates.keychain && rvm osx-ssl-certs update all ; fi

install:
  - if [ "$TRAVIS_OS_NAME" == "linux" ]; then docker build -f Dockerfile-${DOCKER_OS}-qt${QT_VER}-py${TRAVIS_PYTHON_VERSION} -t fredrikaverpil/pyside2-${DOCKER_OS}-qt${QT_VER}-py${TRAVIS_PYTHON_VERSION} . ; fi
  - if [ "$TRAVIS_OS_NAME" == "linux" ]; then docker run --rm -v $(pwd):/pyside-setup/dist fredrikaverpil/pyside2-${DOCKER_OS}-qt${QT_VER}-py${TRAVIS_PYTHON_VERSION} ; fi
  - if [ "$TRAVIS_OS_NAME" == "osx" ]; then chmod +x build_osx_qt${QT_VER}_py${PYTHON_VERSION}.sh ; fi
  - if [ "$TRAVIS_OS_NAME" == "osx" ]; then ./build_osx_qt${QT_VER}_py${PYTHON_VERSION}.sh ; fi
  - if [ "$TRAVIS_OS_NAME" == "osx" ]; then mv ~/pyside-setup/dist/*.whl . ; fi

before_script:
  - pwd

script:
  - ls -alh *.whl

after_success:
  - ls -alh

after_failure:
  - ls -alh

before_deploy:
  # Date
  - DEPLOY_DATE=$(date +'%Y-%m-%d')
  - sed -i -e "s/@DATE@/${DEPLOY_DATE}/g" bintray.json
  # Version
  - if [ "$QT_VER" == "5.6" ]; then git clone --recursive --branch 5.6 https://codereview.qt-project.org/pyside/pyside-setup ; fi
  - PYSIDE_VERSION=$(awk '/^__version__/{print $NF}' pyside-setup/setup.py | tr -d '"')
  - sed -i -e "s/@VERSION@/${PYSIDE_VERSION}/g" bintray.json
  # Package name
  - echo $TRAVIS_BRANCH
  - if [ "$TRAVIS_BRANCH" == "master" ]; then PACKAGE_NAME=pyside2 ; fi
  - if [ "$TRAVIS_BRANCH" != "master" ]; then PACKAGE_NAME=development ; fi
  - echo $PACKAGE_NAME
  - sed -i -e "s/@PACKAGE_NAME@/${PACKAGE_NAME}/g" bintray.json
  # Package description
  - if [ "$TRAVIS_BRANCH" == "master" ]; then PACKAGE_DESC="PySide2 wheels" ; fi
  - if [ "$TRAVIS_BRANCH" != "master" ]; then PACKAGE_DESC="PySide2 wheels from development branches" ; fi
  - sed -i -e "s/@PACKAGE_DESC@/${PACKAGE_DESC}/g" bintray.json
  # Upload folder: master branch
  - if [ "$TRAVIS_BRANCH" == "master" ] && [ "$TRAVIS_OS_NAME" == "linux" ]; then UPLOAD_PATH=$DOCKER_OS/ ; fi
  - if [ "$TRAVIS_BRANCH" == "master" ] && [ "$TRAVIS_OS_NAME" == "osx" ]; then UPLOAD_PATH=osx$(sw_vers -productVersion)/ ; fi
  # Upload folder: non-master branch
  - TRAVIS_BRANCH_REPLACED=$(echo "$TRAVIS_BRANCH" | tr " " _ | tr "/" _)
  - if [ "$TRAVIS_BRANCH" != "master" ] && [ "$TRAVIS_OS_NAME" == "linux" ]; then UPLOAD_PATH=$TRAVIS_BRANCH_REPLACED/$DOCKER_OS/ ; fi
  - if [ "$TRAVIS_BRANCH" != "master" ] && [ "$TRAVIS_OS_NAME" == "osx" ]; then UPLOAD_PATH=$TRAVIS_BRANCH_REPLACED/osx$(sw_vers -productVersion)/ ; fi
  - echo $UPLOAD_PATH
  - sed -i -e "s|@UPLOAD_PATH@|${UPLOAD_PATH}|g" bintray.json


deploy:
  - provider: bintray
    user: fredrikaverpil
    file: bintray.json
    key:
      # api key from https://bintray.com/profile/edit
      # encrypted using travis cli tool:
      # travis encrypt BINTRAY-API-KEY --add deploy.key
      secure: 
    on:
      all_branches: true
      repo: fredrikaverpil/pyside2-wheels
    override: true

after_deploy:
  - ls -alh
  - tree

after_script:
  - ls -alh

